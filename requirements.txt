anthropic>=0.40.0
python-dotenv>=1.0.0
chromadb>=0.5.0
numpy>=1.24.0
faster-whisper>=1.0.0
kokoro-onnx>=0.6.0
silero-vad>=6.0.0
sounddevice>=0.5.0
torch>=2.0.0
mlx-lm>=0.31.0  # --backend local; Apple Silicon only (used the exact repos benchmarked in docs/model-benchmark.md)
fastapi>=0.115.0  # web UI (scripts/web_ui.py)
uvicorn>=0.30.0   # web UI server
python-multipart>=0.0.9  # web UI voice upload (/api/voice)
pypdf>=4.0.0     # book-reading pipeline (scripts/summarize_book.py) - PDF text extraction
ebooklib>=0.18   # book-reading pipeline - EPUB text extraction
sentence-transformers>=3.0.0  # memory retrieval embedding model (see docs/design.md)
