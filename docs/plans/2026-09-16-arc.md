# Arc: the loop for Duc, then Father, then the headset, then polish (2026-09-16)

Duc's instruction, 2026-09-15 night: keep going until blocked; finish the working loop for him and for
Father, then the Vision Pro app and its functionality, then make the whole thing feel like one well-made
app. Claude Fable 5.1 leads and writes red tests; Codex builds one bounded assignment at a time; neither
signs off its own work. Ponytail on both sides: minimum code, few meaningful tests.

Still Duc's alone, unchanged: buying or configuring a third company, approving the automation policy,
choosing Father's first real workflow and providers, any real Father data, merging or pushing.

## Order and assignments

One Codex assignment is live at a time; Claude writes the next red tests on another worktree meanwhile.
Each row: id, worktree, one-line acceptance. Status is edited in place.

| Id | Worktree / branch | Slice | Acceptance in one line | Status |
|---|---|---|---|---|
| A01 | working-loop | Usage ledger over stored receipts | `tests/test_working_loop_usage.py` green; USAGE section on /loop; unknown usage never zero | assigned |
| A02 | working-loop | Declared tier per run and the review rule it implies | Tier stored on the run; Work and Life-changing refuse owner approval without a completed cross-company review; Casual needs one check; rule table in code, one file | next |
| A03 | working-loop | Assignment records | A run can carry a bounded assignment (goal, allowed files, acceptance) and a result hash; the page shows request, result and reviews as one card | after A02 |
| F01 | father | Tenant seam | `KYRA_TENANT=father` refuses a data dir inside the personal one, refuses training scripts, forbids the router and local providers; personal behaviour unchanged | red tests next |
| F02 | father | Deterministic document QA | Parse every DOCX part, flag dashes, provider names, placeholders, metadata; render pages or report render unavailable loudly; facts matched to a source table | red tests next |
| F03 | father | Task cards page | A /father page: start, in progress, review, approve or request changes, history; synthetic workflow only; provider names in an admin view | after F02 |
| V01 | vision-orb-interface | Native orb and card | Plan acceptance lines; simulator states idle, listening, speaking, interrupted, failed | after A01 |
| V02 | vision-orb-interface | Loop and Today on the headset | The Talk tab shows which model answered and lets Duc ask for a cross-company review from the card | after V01 |
| P01 | console | CONSOLE panel (existing plan 2026-09-13) | `tests/test_console.py` green; one place for tools, agents and runs | after A03 |
| P02 | tbd | Design pass across HUD, /loop, /father | One token set, one navigation, one presence vocabulary; phone width clean | last |

## Verify per phase

- Loop: a real two-provider task with a declared tier, a refused approval before review, a ledger row
  that matches the stored receipts. Entry in `docs/log/verification-history.md`.
- Father: synthetic DOCX through F02 with a planted dash, a planted provider name and one wrong fact;
  all three caught; the render step either produces page images or says it could not, never silence.
- Headset: simulator run through the five presence states with the card hidden and Stop still one tap.
- Polish: 375 px and headset screenshots with no clipped controls; `pytest`, `ruff`, Swift tests green.

## Working rules for this arc

- Assignments live in `data/private_docs/assignment-<id>-*.md` in the owning worktree; results in
  `assignment-<id>-result.md`. Claude reads the result and the diff, never the summary alone.
- A finding is fixed by the author in the same slice; a second pass needs a concrete unresolved issue.
- Every real run cleans its pollution and names it.
