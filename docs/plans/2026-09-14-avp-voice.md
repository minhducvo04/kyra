# Native AVP voice input

Duc requested microphone input in the running headset app. Reuse the Mac's existing
`/api/voice/stream` pipeline and Kokoro audio queue. Tap Talk to record, tap Send voice
to upload; Cancel discards. At one minute, stop capture and preserve the clip for explicit Send or Cancel. No wake word or continuous listening in this slice.

- Add recording lifecycle guards before implementation -> verify native tests for permission cancellation, duplicate finish, failed start and discard.
- Record bounded mono WAV on device and upload to the existing voice stream -> verify multipart bytes and transcript/audio/done/error parsing, including missing final events.
- Connect the Talk controls, listening state and cancellation -> verify real visionOS build; no capture after tab/window departure or audio-session interruption, no late playback after Stop.
- Install using Duc's existing signing team -> verify actual headset permission, recording, transcription and spoken reply; preserve evidence privately and distinguish user acceptance from automated checks.
- Run Swift tests, Python suite and lint -> record results, commit only this branch, and hand off for independent review. No merge or push.

## Build verification

22 native tests passed. Python: 643 passed, one existing optional skip. Ruff clean.
The signed app built and installed on the paired AVP. Actual Swift voice transport passed against
an isolated real backend with cached Whisper and BGE directories, Claude, Chroma and Kokoro.
The microphone duration limit preserves the recording. Final headset listening acceptance is pending.
The implementation is isolated on `session/2026-09-14-avp-voice`; the main checkout's signing edits
and unrelated learning-reels work were left untouched. No merge or push.

## Independent review

Claude Opus 5 reviewed the native source and approved the follow-up fixes. The review prompted
preserving time-limited recordings, explicit playback configuration, newline-preserving SSE parsing,
and leaving completed reply badges unchanged when opening settings. The SSE regression failed before
the correction and passed afterwards. Full records are in the private verification directory.
Final signed build, installation and launch all exited successfully. Headset listening acceptance
remains Duc's check; automated success does not establish audible playback on the physical device.
